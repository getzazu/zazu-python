# Copy to .env (gitignored). Used only for cassette replay tests.
# zazu-python does not record cassettes — those come from zazu-ruby's
# release tarball. See scripts/fetch_cassettes.py.
ZAZU_API_KEY=
